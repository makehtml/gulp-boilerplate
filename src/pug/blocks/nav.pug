nav.nav
  a.nav__logo(href=mainPage? null:nav.logo.href)
    img(src=nav.logo.src alt=nav.logo.name width=nav.logo.width height=nav.logo.height)
  include menu.pug
  button.hamburger.hamburger--collapse
    span.hamburger-box
      span.hamburger-inner
